import React from "react";

const SearchBar = () => {
  return (
    <input
      className="w-full py-2.5 px-4 bg-primary-950 border border-primary-700 text-primary-100 rounded-lg"
      type="text"
      placeholder="Cari Catatan"
    />
  );
};

export default SearchBar;
